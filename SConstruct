VariantDir("build", "src")

name = "relief_mapping"
files = Glob("build/*.cpp")
libs = ["GL", "GLU", "GLEW", "sfml-window", "sfml-system", "sfml-graphics", "guichan", "guichan_opengl"]

Program(name, files, LIBS=libs, CCFLAGS="-g")

